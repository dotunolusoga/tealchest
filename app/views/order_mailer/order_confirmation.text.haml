Hello
= @order.user.name,

Thank you for placing an order. Here is a summary for order # = @order_id:

- @order.items.each do |item| do
    = item.quantity
    = item.product.name
    = ( number_to_currency item.total_price)
- end

Total:
= number_to_currency @order.total_price
